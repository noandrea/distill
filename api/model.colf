// Package demo offers a demonstration.
package iljl

// Course is the grounds where the game of golf is played.
type URLReq struct {
  ID            text `yaml:"id"`
	URL           text
	TTL           int64
	MaxRequests   int64
}

// Course is the grounds where the game of golf is played.
type URLInfo struct {
  ID            text
	URL           text
	Counter       int64
  TTL           int64
  MaxRequests   int64
	BountAt       timestamp
}
