// Package urlstore contains the engine of distill
package urlstore

// URLInfo contains the data associated to an id
type URLInfo struct {
  ID            text
	URL           text
	Counter       uint64
	BountAt       timestamp
  MaxRequests   uint64
  ExhaustedURL  text
  TTL           uint64
  ExpireOn      timestamp
  ExpiredURL    text
}
